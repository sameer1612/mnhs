1. paste the project folder contents in this folder, not the project folder itself.

2. move back to root folder "mnhs/"

3. run : git add .

4. run : git commit -m "initial_mob"

5. run : git push origin master